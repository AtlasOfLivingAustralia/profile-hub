AWSTemplateFormatVersion: "2010-09-09"

Description: Profiles UI Infrastructure

Parameters:
  pProfilesWafWebAclArn:
    Description: The WAF Web ACL to attach the CloudFront distribution to
    Type: String

  pSourceBucket:
    Description: The bucket name for the site
    Type: String

  pBucketPath:
    Description: Optional path that the files are stored under
    Type: String

  pCleanBranch:
    Description: A cleaned version of the code branch name
    Type: String
    Default: development

  pEnvironment:
    Description: The AWS environment this belongs to
    Type: String

  pHostedZone:
    Description: The hosted zone the site are accessed under
    Type: String

  pSubDomain:
    Description: The subdomain the site are accessed on
    Type: String

  pSslCertificate:
    Description: The arn of the SSL certificate to be used
    Type: String

Conditions:
  IsDev: !Equals
    - !Ref pEnvironment
    - development

  NotDev: !Not
    - !Condition IsDev

Resources:
  ProfilesBucket:
    DeletionPolicy: Delete
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub ${pSourceBucket}
      PublicAccessBlockConfiguration:
        BlockPublicAcls: TRUE
        BlockPublicPolicy: TRUE
        IgnorePublicAcls: TRUE
        RestrictPublicBuckets: TRUE
      Tags:
        - Key: component
          Value: website
        - Key: Name
          Value: !Sub ${pSourceBucket}

  ProfilesBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref ProfilesBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowCloudFrontService
            Action:
              - s3:GetObject
            Effect: Allow
            Resource: !Sub arn:aws:s3:::${ProfilesBucket}/*
            Principal:
              AWS: !Sub arn:aws:iam::cloudfront:user/CloudFront Origin Access Identity ${ProfilesCloudFrontOai}
          - Sid: TLSAccessOnly
            Action:
              - s3:*
            Effect: Deny
            Resource:
              - !Sub arn:aws:s3:::${ProfilesBucket}
              - !Sub arn:aws:s3:::${ProfilesBucket}/*
            Principal: '*'
            Condition:
              Bool:
                aws:SecureTransport: "false"

  ProfilesDnsRecord:
    Type: AWS::Route53::RecordSet
    Properties:
      Name: !Sub ${pSubDomain}.${pHostedZone}
      Comment: !Sub Profiles UI domain for the ${pEnvironment} environment
      Type: A
      AliasTarget:
        DNSName: !GetAtt ProfilesCloudFrontDistro.DomainName
        HostedZoneId: Z2FDTNDATAQYW2
      HostedZoneName: !Sub ${pHostedZone}.

  ProfilesCloudFrontDistro:
    Type: AWS::CloudFront::Distribution
    DependsOn: ProfilesBucket
    Properties:
      DistributionConfig:
        Aliases:
          - !Sub ${pSubDomain}.${pHostedZone}
        Comment: !Sub CF Distribution for the Profiles UI ${pEnvironment}
        CustomErrorResponses:
          - ErrorCode: 403
            ErrorCachingMinTTL: 300
            ResponseCode: 200
            ResponsePagePath: /index.html
        DefaultCacheBehavior:
          AllowedMethods:
            - GET
            - HEAD
          CachedMethods:
            - GET
            - HEAD
          CachePolicyId: !Ref ProfilesCachePolicy
          Compress: true
          TargetOriginId: profiles-s3
          ViewerProtocolPolicy: redirect-to-https
        DefaultRootObject: index.html
        Enabled: true
        HttpVersion: http2
        IPV6Enabled: false
        Origins:
          - DomainName: !Sub ${pSourceBucket}.s3.${AWS::Region}.amazonaws.com
            Id: profiles-s3
            S3OriginConfig:
              OriginAccessIdentity: !Sub origin-access-identity/cloudfront/${ProfilesCloudFrontOai}
            OriginPath: !Sub /${pBucketPath}
        ViewerCertificate:
          AcmCertificateArn: !Ref pSslCertificate
          MinimumProtocolVersion: TLSv1.2_2021
          SslSupportMethod: sni-only
        WebACLId: !Ref pProfilesWafWebAclArn
      Tags:
        - Key: component
          Value: cdn

  ProfilesCachePolicy:
    Type: AWS::CloudFront::CachePolicy
    Properties:
      CachePolicyConfig:
        Comment: !Sub CF Cache Policy for the Profiles UI ${pEnvironment}
        DefaultTTL: 300
        MaxTTL: 86400
        MinTTL: 1
        Name: !Sub
          - Profiles-cache-Policy-${ResourceName}
          - ResourceName: !If
              - IsDev
              - !Ref pCleanBranch
              - !Ref pEnvironment
        ParametersInCacheKeyAndForwardedToOrigin:
          CookiesConfig:
            CookieBehavior: none
          EnableAcceptEncodingBrotli: true
          EnableAcceptEncodingGzip: true
          HeadersConfig:
            HeaderBehavior: none
          QueryStringsConfig:
            QueryStringBehavior: whitelist
            QueryStrings:
              - cb

  ProfilesCloudFrontOai:
    Type: AWS::CloudFront::CloudFrontOriginAccessIdentity
    Properties:
      CloudFrontOriginAccessIdentityConfig:
        Comment: !Sub CF Origin Access Identity for the Profiles UI ${pEnvironment}

Outputs:
  ProfilesBucketName:
    Value: !Ref ProfilesBucket

  ProfilesBucketArn:
    Value: !GetAtt ProfilesBucket.Arn

  ProfilesCloudFrontDistributionArn:
    Value: !Sub
      - arn:aws:cloudfront::${AWS::AccountId}:distribution/${DistributionId}
      - DistributionId: !GetAtt ProfilesCloudFrontDistro.Id
